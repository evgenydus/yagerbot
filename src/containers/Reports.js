import React from 'react'
import { observer } from 'mobx-react'

import PageWrapper from '../components/PageWrapper'

const Reports = ({ store }) => <PageWrapper>Отчеты</PageWrapper>

export default observer(Reports)
